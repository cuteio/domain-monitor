{
  "name": "domain-monitor",
  "version": "1.0.0",
  "description": "A monitoring tool that tracks domain names and SSL certificates expiration dates and sends timely reminders before they expire.",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js"
  },
  "keywords": [
    "domain",
    "monitor",
    "ssl",
    "expiration",
    "github",
    "actions"
  ],
  "license": "MIT",
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@vercel/ncc": "^0.38.2",
    "date-fns": "^2.30.0",
    "extract-domain": "^2.4.8",
    "ssl-checker": "^2.0.8"
  }
}
